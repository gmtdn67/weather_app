# Weather Forecast App

Веб-приложение для просмотра прогноза погоды в любом городе мира на текущее время, ближайшие часы и дни вперед.

## Технологический стек

- **React 18+** с TypeScript
- **Redux Toolkit** + **RTK Query** для state management и API
- **Chakra UI** для UI компонентов
- **Framer Motion** для анимаций
- **i18next** для многоязычности
- **Vite** для сборки
- **Open-Meteo API** для данных о погоде
- **Nominatim** для геокодирования

## Архитектура

Проект использует **Feature-Sliced Design (FSD)**:

```
src/
├── app/          # Инициализация приложения (провайдеры, роутинг)
├── pages/        # Страницы приложения
├── widgets/      # Композитные UI компоненты
├── features/     # Бизнес-логика фич
├── entities/     # Бизнес-сущности
└── shared/       # Переиспользуемые компоненты и утилиты
```

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview
```

## Функциональность

- 🔍 Поиск погоды по названию города
- 📍 Автоматическое определение местоположения
- 📊 Почасовой прогноз на 24 часа
- 📅 Ежедневный прогноз на 7-14 дней
- 🌡️ Переключение единиц измерения (°C/°F)
- 🌓 Темная/светлая тема
- 🌐 Поддержка русского и английского языков
- 📝 История поиска (последние 5-10 городов)
- ✨ Плавные анимации переходов

## Структура проекта

```
src/
├── app/              # Инициализация приложения
│   ├── App.tsx       # Главный компонент
│   ├── store.ts      # Redux store
│   ├── theme.ts      # Chakra UI тема
│   └── i18n.ts       # i18next конфигурация
├── pages/            # Страницы
│   ├── HomePage.tsx  # Страница поиска
│   └── ResultsPage.tsx # Страница результатов
├── widgets/          # Композитные компоненты
│   ├── WeatherCard/   # Карточка текущей погоды
│   ├── HourlyForecast/ # Почасовой прогноз
│   ├── DailyForecast/  # Ежедневный прогноз
│   ├── SearchHistory/  # История поиска
│   └── Settings/       # Настройки
├── features/         # Бизнес-логика
│   ├── search/       # Поиск города
│   ├── geolocation/  # Определение местоположения
│   └── settings/     # Настройки приложения
├── entities/         # Бизнес-сущности
│   ├── city/         # Модель города и геокодирование
│   └── weather/      # Модель погоды и API
└── shared/           # Общие компоненты и утилиты
    ├── ui/           # UI компоненты
    ├── lib/          # Утилиты
    └── i18n/         # Переводы
```

## Деплой

Приложение готово к деплою на Vercel или Netlify:

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## Примечания

- При первом запуске выполните `npm install` для установки зависимостей
- Приложение использует Open-Meteo API (бесплатный, без API-ключей)
- Геокодирование выполняется через Nominatim (OpenStreetMap)
- Все настройки и история поиска сохраняются в localStorage

